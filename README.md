# pH Sensor Extension for MicroBlock

Extension สำหรับอ่านค่า pH จาก Analog pH Sensor พร้อมฟังก์ชันคาลิเบรท

## Features
- อ่านค่า pH (0-14)
- อ่านค่าแรงดันจาก pH Sensor (0-3.3V)
- ตั้งค่า Offset สำหรับคาลิเบรท

## Supported Boards
- ESP32
- Arduino

## Installation
ติดตั้งผ่าน MicroBlock IDE Extension Manager

## Blocks

### 1. **อ่านค่า pH (ขา A)** - Reporter Block
```
[อ่านค่า pH (ขา A [34])]
```
- ต่อกับ block ที่รับค่าตัวเลข เช่น แสดงผล LCD, เก็บในตัวแปร, เงื่อนไข if

**ตัวอย่าง:**
```
ตั้งค่าตัวแปร pH_value เป็น [อ่านค่า pH (ขา A [34])]
```

### 2. **อ่านค่าแรงดัน pH (ขา A)** - Reporter Block
```
[อ่านค่าแรงดัน pH (ขา A [34])]
```
- ใช้ตรวจสอบแรงดันที่อ่านได้จาก sensor

**ตัวอย่าง:**
```
แสดงผล [อ่านค่าแรงดัน pH (ขา A [34])] บน LCD
```

### 3. **ตั้งค่า pH Offset** - Command Block
```
[ตั้งค่า pH Offset [0]]
```
- ใช้สำหรับคาลิเบรท sensor

## 🔧 วิธีการคาลิเบรท pH Sensor

### ขั้นตอน:
1. **แช่ pH sensor ใน pH Buffer 7.0**
2. **อ่านค่าที่ได้** (เช่น 7.5)
3. **คำนวณ Offset** = 7.0 - 7.5 = -0.5
4. **ตั้งค่า Offset:**
   ```
   ตั้งค่า pH Offset [-0.5]
   ```

### ตัวอย่างโปรแกรม:

```
เมื่อเริ่มต้น
├─ ตั้งค่า pH Offset [0]
└─ ทำซ้ำตลอด
   ├─ ตั้งค่าตัวแปร pH เป็น [อ่านค่า pH (ขา A [34])]
   ├─ แสดงผล pH บน Serial Monitor
   └─ รอ 1000 ms
```

### ตัวอย่างการใช้งานพร้อมเงื่อนไข:

```
ทำซ้ำตลอด
├─ ตั้งค่าตัวแปร pH เป็น [อ่านค่า pH (ขา A [34])]
├─ ถ้า pH < 6.5
│  └─ แสดงข้อความ "น้ำเป็นกรด"
├─ ถ้าไม่ ถ้า pH > 7.5
│  └─ แสดงข้อความ "น้ำเป็นเบส"
├─ ถ้าไม่
│  └─ แสดงข้อความ "น้ำเป็นกลาง"
└─ รอ 1000 ms
```

## Hardware Connection
- VCC → 3.3V หรือ 5V
- GND → GND
- Analog Out → GPIO34 (หรือ Analog Pin อื่นๆ)

## Author
cap_apiluk

## License
MIT
